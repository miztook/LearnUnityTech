1. houdini场景的mevmesh现有规则和生成
	增加调试命令显示现有的navmesh对象
	
2. 现有游戏问题
	1. 玩家部队，移动路径有问题，通知服务器检查1		
	2. 服务器发错数值：客户端兼容处理
	3. 回包错误码: 1, Tag: 406, ErrorInfo:			//提示消息异常
	4. Combine mesh instance 60 is null.			//大量报
	
3. 大地图场景加载策略  
	美术场景(位置移动时，加载卸载，在快速移动时处理大量物体)，逻辑物体(位置改变时，发送协议，服务器同步对象加载卸载)
	地表:
	远景:
	近景物体:
	近景植被:
	
4. 性能问题
	SceneObject			(已解决)
		rss_root, lod3_root，在镜头拉远时显示图标，rss_root的近景不需要显示，否则面数过高
		
	AnimationBase在更新时的 LoadObject 问题
	
5. 待做
		覆盖安装时配置是否清理 
		cdn配置版本号，旧版本开始时强制更新
		
		
		
		